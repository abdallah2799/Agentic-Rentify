# 🏗️ Image Management System - Architecture Diagram

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                          FRONTEND (React/Vue)                        │
│                                                                       │
│  ┌──────────────────┐    ┌──────────────────┐    ┌──────────────┐  │
│  │ Upload Component │    │ Gallery Component│    │ Delete Dialog│  │
│  └────────┬─────────┘    └────────┬─────────┘    └──────┬───────┘  │
│           │                        │                      │           │
└───────────┼────────────────────────┼──────────────────────┼───────────┘
            │                        │                      │
            ▼                        ▼                      ▼
    ┌───────────────┐        ┌──────────────┐      ┌──────────────────┐
    │ POST /upload/ │        │ POST /upload/│      │  POST /upload/   │
    │ photo         │        │ photo        │      │  delete          │
    │ (multipart)   │        │ (multipart)  │      │ (application/json)
    └───────────────┘        └──────────────┘      └──────────────────┘
            │                        │                      │
            └────────────────────────┴──────────────────────┘
                                    │
                                    ▼
        ┌───────────────────────────────────────────────────────┐
        │         API LAYER (UploadController)                  │
        │                                                        │
        │  ┌────────────────────────────────────────────────┐  │
        │  │ UploadPhoto(file)   → AddPhotoAsync           │  │
        │  │ DeletePhoto(request) → DeletePhotoCommand     │  │
        │  │ UploadLogo(file)    → Deprecated (Legacy)     │  │
        │  └────────────────────────────────────────────────┘  │
        └───────────────────┬──────────────────────────────────┘
                            │
        ┌───────────────────┴──────────────────────┐
        │                                           │
        ▼                                           ▼
┌──────────────────────────┐          ┌─────────────────────────────┐
│  IPhotoService           │          │  MediatR Pipeline           │
│                          │          │                             │
│  ┌────────────────────┐  │          │ ┌─────────────────────────┐│
│  │ AddPhotoAsync()    │  │          │ │ DeletePhotoCommand      ││
│  │ • Validate file    │  │          │ ├─────────────────────────┤│
│  │ • Upload to        │  │          │ │ DeletePhotoValidator    ││
│  │   Cloudinary       │  │          │ │ • Validate PublicId     ││
│  │ • Return URL +     │  │          │ ├─────────────────────────┤│
│  │   PublicId         │  │          │ │ DeletePhotoCommandHandler
│  │                    │  │          │ │ • Call IPhotoService    ││
│  │ DeletePhotoAsync() │  │          │ │ • Delete from Cloudinary││
│  │ • Delete from      │  │          │ └─────────────────────────┘│
│  │   Cloudinary       │  │          └─────────────────────────────┘
│  │ • Return success   │  │
│  └────────────────────┘  │
└──────────────────────────┘
        │           │
        │           └─────────────────────┐
        │                                  │
        ▼                                  ▼
┌────────────────────────┐      ┌──────────────────────────────┐
│  Cloudinary SDK        │      │  Service Container (DI)      │
│                        │      │                              │
│ ┌──────────────────┐   │      │  ┌──────────────────────┐   │
│ │ UploadAsync()    │   │      │  │ AddScoped<IPhoto     │   │
│ │ DestroyAsync()   │   │      │  │   Service,           │   │
│ │ ListResources()  │   │      │  │   PhotoService>      │   │
│ └──────────────────┘   │      │  │                      │   │
└────────────────────────┘      │  │ AddScoped<IImageClean│   │
                                │  │   upService,         │   │
                                │  │   ImageCleanupService
                                │  └──────────────────────┘   │
                                └──────────────────────────────┘
```

---

## Data Flow Diagram

### Image Upload Flow
```
User selects file
       │
       ▼
File validation (client-side)
       │
       ▼
POST /api/Upload/photo (multipart/form-data)
       │
       ▼
UploadController.UploadPhoto()
       │
       ▼
IPhotoService.AddPhotoAsync()
       │
       ▼
Cloudinary Upload
       │
       ▼
Return: PhotoResponseDTO
       {
         "url": "https://res.cloudinary.com/...",
         "publicId": "folder/image-001"
       }
       │
       ▼
Frontend stores URL in component state
Frontend sends URL + publicId to entity creation endpoint
       │
       ▼
Database stores: mainImage: URL, images: [URLs...]
```

### Image Deletion Flow (CQRS)
```
User clicks delete
       │
       ▼
POST /api/Upload/delete { publicId }
       │
       ▼
UploadController.DeletePhoto(request)
       │
       ▼
MediatR.Send(DeletePhotoCommand)
       │
       ▼
DeletePhotoCommandValidator
       (Validates publicId)
       │
       ▼
DeletePhotoCommandHandler
       │
       ▼
IPhotoService.DeletePhotoAsync()
       │
       ▼
Cloudinary Destroy
       │
       ▼
Return: Success/Failure
       │
       ▼
Response to client: { success: true/false }
```

### Automatic Cleanup Flow (Hangfire)
```
┌─────────────────────────────────────────────────────┐
│   Hangfire Scheduler (Every hour UTC)               │
│   Job ID: "image-cleanup-hourly"                    │
└──────────────┬──────────────────────────────────────┘
               │
               ▼
   IImageCleanupService.CleanupOrphanedImagesAsync()
               │
         ┌─────┴─────┐
         │           │
         ▼           ▼
   Get used      Get all
   PublicIds     Cloudinary
   from DB       PublicIds
   │
   ├─ Query Trips.Images
   ├─ Query Attractions.Images
   ├─ Query Hotels.Images
   ├─ Query Cars.Images
   │
   └─ Extract PublicIds from URLs
       using regex patterns
         │
         ▼
   Compare sets:
   OrphanedIds = CloudinaryIds - UsedIds
         │
         ▼
   For each orphaned PublicId:
   ├─ Cloudinary.DestroyAsync(publicId)
   ├─ Log success/failure
   └─ Increment delete counter
         │
         ▼
   Log final report:
   "Deleted N orphaned images"
```

---

## Class Diagram

```
┌─────────────────────────┐
│   IPhotoService         │
│                         │
│ + AddPhotoAsync()       │
│ + DeletePhotoAsync()    │
└──────────┬──────────────┘
           │
           │ implements
           │
           ▼
┌──────────────────────────────────────┐
│   PhotoService                       │
│   (Infrastructure.Services)          │
│                                      │
│ - cloudinary: Cloudinary             │
│                                      │
│ + AddPhotoAsync(): PhotoResponseDTO  │
│ + DeletePhotoAsync(): bool           │
└──────────────────────────────────────┘
           │ uses
           ▼
    ┌─────────────────┐
    │ Cloudinary SDK  │
    │ ImageUploadParams
    │ DeletionParams  │
    └─────────────────┘

┌─────────────────────────────────────┐
│   IImageCleanupService              │
│                                     │
│ + CleanupOrphanedImagesAsync(): int │
└──────────────┬──────────────────────┘
               │
               │ implements
               │
               ▼
┌──────────────────────────────────────────────┐
│   ImageCleanupService                        │
│   (Infrastructure.Services)                  │
│                                              │
│ - cloudinary: Cloudinary                     │
│ - unitOfWork: IUnitOfWork                    │
│ - logger: ILogger<ImageCleanupService>       │
│                                              │
│ + CleanupOrphanedImagesAsync(): int          │
│ - GetAllUsedPublicIdsAsync(): HashSet<str>   │
│ - GetAllCloudinaryPublicIdsAsync(): HashSet  │
│ - ExtractPublicIdFromUrl(string): string     │
│ - DeleteFromCloudinaryAsync(string): bool    │
└──────────────────────────────────────────────┘
           │ uses
           └───────────────┬────────────────┐
                           │                │
                           ▼                ▼
                    IUnitOfWork      Cloudinary SDK
                    (Database)       ListResourcesParams

┌────────────────────────────────────────────┐
│   DeletePhotoCommand                       │
│   (Application.Features.Photos.Commands)   │
│                                            │
│ - PublicId: string                         │
│                                            │
│ : IRequest<bool>                           │
└────────────────────────────────────────────┘
           │ handled by
           ▼
┌────────────────────────────────────────────────┐
│   DeletePhotoCommandHandler                    │
│   (Application.Features.Photos.Commands)      │
│                                                │
│ - photoService: IPhotoService                  │
│                                                │
│ + Handle(DeletePhotoCommand): Task<bool>       │
│   : IRequestHandler<DeletePhotoCommand, bool>  │
└────────────────────────────────────────────────┘
           │ uses
           ▼
        IPhotoService

┌────────────────────────────────────────────────┐
│   DeletePhotoCommandValidator                  │
│   (Application.Features.Photos.Commands)      │
│                                                │
│ : AbstractValidator<DeletePhotoCommand>        │
│                                                │
│ + DeletePhotoCommandValidator()                │
│   (Fluent rules configuration)                 │
└────────────────────────────────────────────────┘

┌────────────────────────────────────┐
│   PhotoResponseDTO                 │
│   (Application.Wrappers)           │
│                                    │
│ - Url: string                      │
│ - PublicId: string                 │
└────────────────────────────────────┘
           │ returned by
           ▼
      IPhotoService.AddPhotoAsync()

┌──────────────────────────────────┐
│   UploadController               │
│   (Api.Controllers)              │
│                                  │
│ - photoService: IPhotoService    │
│ - mediator: IMediator            │
│                                  │
│ + UploadPhoto(file)              │
│ + DeletePhoto(request)           │
│ + UploadLogo(file) [Deprecated]  │
└──────────────────────────────────┘
```

---

## Layer Dependencies

```
┌─────────────────────────────────────────────────────┐
│                      API LAYER                       │
│  ┌───────────────────────────────────────────────┐  │
│  │  UploadController                             │  │
│  │  • Handles HTTP requests/responses            │  │
│  │  • Delegates to services & MediatR            │  │
│  └───────────────┬────────────────────┬──────────┘  │
└──────────────────┼────────────────────┼──────────────┘
                   │                    │
        ┌──────────┴────────┐           │
        │                   │           │
        ▼                   ▼           ▼
┌──────────────────────────────────────────────────────┐
│               APPLICATION LAYER                       │
│  ┌────────────────┐  ┌──────────────────────────┐   │
│  │ Interfaces     │  │ Features (CQRS)          │   │
│  │ • IPhotoService│  │ • DeletePhotoCommand     │   │
│  │ • IImageClean  │  │ • DeletePhotoCommandHandler
│  │   upService    │  │ • DeletePhotoCommandValidator
│  │                │  │ • PhotoResponseDTO      │   │
│  └────────────────┘  └──────────────────────────┘   │
└──────────────┬───────────────────────────────────────┘
               │
        ┌──────┴──────────────┐
        │                     │
        ▼                     ▼
┌───────────────────────────────────────────────────────┐
│            INFRASTRUCTURE LAYER                        │
│  ┌──────────────────┐  ┌─────────────────────────┐   │
│  │ Services         │  │ External Dependencies    │   │
│  │ • PhotoService   │  │ • Cloudinary SDK        │   │
│  │ • ImageCleanup   │  │ • IUnitOfWork           │   │
│  │   Service        │  │ • ILogger               │   │
│  │                  │  │ • Hangfire              │   │
│  └──────────────────┘  └─────────────────────────┘   │
└───────────────────────────────────────────────────────┘
               │
        ┌──────┴──────────────┐
        │                     │
        ▼                     ▼
    Database           Cloudinary API
    (SQL Server)       (Image Storage)
```

---

## MediatR Pipeline (CQRS Request Flow)

```
DeletePhotoCommand
       │
       ▼
MediatR Pipeline
       │
       ├─ Validation Behavior
       │  └─ DeletePhotoCommandValidator
       │     ├─ Check PublicId not empty
       │     └─ Check PublicId length ≤ 500
       │
       ├─ Logging Behavior (optional)
       │
       ▼
DeletePhotoCommandHandler
       │
       ├─ Dependency: IPhotoService
       │
       ▼
IPhotoService.DeletePhotoAsync(publicId)
       │
       ├─ Validate publicId
       ├─ Call Cloudinary DestroyAsync
       ├─ Handle exceptions
       │
       ▼
Return: bool (success/failure)
```

---

## Database Entity Relationships

```
┌──────────────────┐
│      Trip        │
├──────────────────┤
│ • Id             │
│ • Title          │
│ • MainImage      │◄─── URL to Cloudinary
│ • Images[]       │◄─── List of URLs
│ • ...            │
└──────────────────┘

┌──────────────────┐
│   Attraction     │
├──────────────────┤
│ • Id             │
│ • Name           │
│ • Images[]       │◄─── List of AttractionImage
│   ├─ Url         │     (each has URL to Cloudinary)
│ • ...            │
└──────────────────┘

┌──────────────────┐
│      Hotel       │
├──────────────────┤
│ • Id             │
│ • Name           │
│ • Images[]       │◄─── List of URLs
│ • ...            │
└──────────────────┘

┌──────────────────┐
│       Car        │
├──────────────────┤
│ • Id             │
│ • Name           │
│ • Images[]       │◄─── List of URLs
│ • ...            │
└──────────────────┘

All images stored as URLs in JSON format
PublicIds extracted from URLs for cleanup operations
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────┐
│             Load Balancer (Optional)                │
└──────────────────┬──────────────────────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
        ▼                     ▼
   ┌─────────┐          ┌─────────┐
   │ API Pod │          │ API Pod │  (Scalable)
   │ (AspNet │          │ (AspNet │
   │  Core)  │          │  Core)  │
   └────┬────┘          └────┬────┘
        │                    │
        └────────────┬───────┘
                     │
        ┌────────────┴───────────┐
        │                        │
        ▼                        ▼
   ┌──────────────┐       ┌──────────────┐
   │ SQL Server DB│       │  Redis Cache │
   │              │       │  (Optional)  │
   └──────────────┘       └──────────────┘
        │
        └─────┐
              │
              ▼
        ┌──────────────┐
        │   Hangfire   │
        │   Dashboard  │
        │ & Job Queue  │
        └──────────────┘
        (Runs in-process
         in one API pod)
        │
        ▼
   Cloudinary API
   (Cloud Image Storage)
```

---

## Security & Permissions

```
┌─────────────────┐
│ API Endpoint    │
├─────────────────┤
│ POST /api/Upload│  ← Requires Authorization header
│                 │    (JWT Token)
└────────┬────────┘
         │
    ┌────▼─────┐
    │ Validated│
    │ User     │
    └────┬─────┘
         │
         ▼
   ┌───────────────────────┐
   │ Service Execution     │
   │ • Upload to Cloudinary│
   │ • Store in Database   │
   │ • Clean up orphaned   │
   └───────────────────────┘
```

---

## Monitoring & Logging

```
┌───────────────────────────────────────────────────┐
│            Serilog Logging                         │
│                                                    │
│ ┌────────────────────────────────────────────┐   │
│ │ PhotoService                               │   │
│ │ • Log upload start/success/failure          │   │
│ │ • Log Cloudinary API calls                  │   │
│ ├────────────────────────────────────────────┤   │
│ │ ImageCleanupService                        │   │
│ │ • Log cleanup job start                    │   │
│ │ • Log items found/deleted                  │   │
│ │ • Log errors and exceptions                │   │
│ ├────────────────────────────────────────────┤   │
│ │ UploadController                           │   │
│ │ • Log request/response                     │   │
│ │ • Log validation errors                    │   │
│ └────────────────────────────────────────────┘   │
│                   │                               │
│                   ▼                               │
│  Console | File | Application Insights | ELK   │
└───────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────┐
│            Hangfire Monitoring                     │
│                                                    │
│ Dashboard: http://localhost:5000/hangfire        │
│                                                    │
│ • Job execution history                          │
│ • Recurring job schedule                         │
│ • Success/failure statistics                     │
│ • Job details and logs                           │
└───────────────────────────────────────────────────┘
```

---

## Performance Considerations

```
Upload Performance:
  • Single request handles entire upload
  • 500x500 transformation done server-side
  • Cloudinary caches transformed images
  • Average upload: 100-500ms

Deletion Performance:
  • Fast Cloudinary API call
  • CQRS validation lightweight
  • No database transaction needed

Cleanup Performance:
  • Runs hourly, not blocking
  • Pagination handles large image collections
  • Batch deletion from Cloudinary
  • Database queries use IUnitOfWork caching

Scalability:
  • Services are stateless (scalable)
  • Hangfire SQL Storage supports multiple instances
  • Cloudinary is fully distributed
  • Database pooling configured
```
